# FLUXFORGE Android wrapper

Open this folder in Android Studio, then **Build → Generate Signed App Bundle**.

Game files live in `app/src/main/assets/www/`. After editing the HTML5 game, refresh them with:

```bash
bash ../fluxforge/scripts/sync-android.sh
```

Full Play Store steps: `../fluxforge/PLAY_STORE.md`
